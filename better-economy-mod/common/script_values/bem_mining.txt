bem_mining_efficiency_mult = {
    value = level
    if = {
        limit = {is_building_group = bg_coal_mining}
        prev = {divide = "num_potential_resources(bg:bg_coal_mining)"}
    }
    if = {
        limit = {is_building_group = bg_iron_mining}
        prev = {divide = "num_potential_resources(bg:bg_iron_mining)"}
    }
    if = {
        limit = {is_building_group = bg_sulfur_mining}
        prev = {divide = "num_potential_resources(bg:bg_sulfur_mining)"}
    }
    if = {
        limit = {is_building_group = bg_lead_mining}
        prev = {divide = "num_potential_resources(bg:bg_lead_mining)"}
    }
    multiply = 2
    subtract = 0.3 
}